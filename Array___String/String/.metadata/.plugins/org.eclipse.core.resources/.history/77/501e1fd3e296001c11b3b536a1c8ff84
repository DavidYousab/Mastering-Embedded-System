#include"stdio.h"
#include"string.h"
int main(){
	int i;
	char str1[50],str2[50];

	printf("Enter a string: ");
	fflush(stdin); fflush(stdout);
	scanf("%s",str1);

	for(i=0; i<strlen(str1); i++)
	{
		str2[i]=str1[strlen(str1)-i];
	}

	printf("%s",str2);


	return 0;
}
