#include"stdio.h"
#include"string.h"
int main(){
	int i,len;
	char str1[50],str2[50];

	printf("Enter a string: ");
	fflush(stdin); fflush(stdout);
	scanf("%s",str1);

	len=strlen(str1);

	printf("%s=d",len);


	return 0;
}
