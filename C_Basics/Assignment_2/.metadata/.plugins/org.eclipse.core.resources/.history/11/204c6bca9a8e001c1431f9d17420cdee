#include "stdio.h"

void main()
{
	int num,i,sum=0;
	printf("Enter Number:");
	fflush(stdin); fflush(stdout);
	scanf("%D",&num);

	if(num>0)
	{
		for(i=0; i<=num;i++)
				sum +=i;
	}

	else if(num1<0)
		{
			printf("%f is negative",num1);
		}
	else
		{
			printf("%f is zero",num1);
		}
}
